---
title: "Installation"
description: |
  Installing Packages
---

This page will talk about how users can install packages, it will cover the tools: `pak`, `remotes`, and `install.packages` and describe when to use each. Thge site will link to pages on repositories and libraries (to put package installation in the context of installing from a repo to a library). This page will also cover how to handle system dependencies and the differences between binary and source packages.

# Package Installation
- pak vs remotes
- bootstrapping with install.packages (chicken meet egg)

# Repositories and Libraries 
aka where do they come from, where do they go?
- Links to repos 
- Links to libraries

# Details
- binaries
- caches

# System Dependencies

# Upgrades (renv)
- link to use case